language: c

addons:
  apt:
    packages:
    - texlive-full

script: 
  - make 
  - mkdir docs-tmp
  - cp *.pdf docs-tmp
  - git checkout gh-pages
  - cp docs-tmp/* .
  - git add *.pdf
  - git commit -m "Generated from master"
  - git push

